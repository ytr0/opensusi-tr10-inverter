# ytr0_top — ピン一覧と使い方

OpenSUSI-TR10（TR-1um PDK v1.2609.0）向けのブロックです。
トップセル名は **`ytr0_top`**。枠の中に配置して、下の表のとおりパッドへつないでください。

## バリアント

| 名前 | 中身 | 必要な信号パッド |
|---|---|---|
| `inverter_only` | CMOS インバータ + シリコンアート | 2本（A, Q） |
| `full` | 上記 + 1ビット SRAM | 4本（A, Q, DATA, WORD） |

パッドに余裕がなければ `inverter_only` を使ってください。**GDS を編集する必要はありません。**
どちらも DRC 違反 0 件、LVS 一致を確認済みです。

## ピン

| ピン | 種類 | レイヤー | 説明 |
|---|---|---|---|
| `A` | 入力 | M1 | インバータの入力。0V / 5V のロジック |
| `Q` | 出力 | M1 | インバータの出力。負荷 100fF で遅延 0.8ns |
| `VDD` | 電源 | M1 | 5V |
| `VSS` | 電源 | M1 | 0V |
| `DATA` | 入出力 | M1 | SRAM のデータ（`full` のみ） |
| `WORD` | 入力 | **M2** | SRAM のアクセス制御（`full` のみ） |

`WORD` だけ M2 です。ほかは M1 です。

## 使い方

### インバータ
`A` に 0V / 5V を入れると、`Q` に反転した値が出ます。
`full` の場合は、**`WORD` を 0V にしておいてください**（SRAM を切り離すため）。

### SRAM（`full` のみ）

| 操作 | DATA | WORD |
|---|---|---|
| 書く | 0V か 5V を駆動する | 5V |
| 保持 | 開放 | 0V |
| 読む | 開放にして電圧を測る | 5V |

- 読み出しは非破壊です。値は壊れません
- 読むときは `DATA` を必ず開放にしてください。駆動したままだと書き込みになります
- 電源を切ると内容は消えます（不揮発ではありません）
- 電源投入直後の値は決まっていません

## 統合後に確認してほしいこと

1. チップ全体で DRC
2. チップ全体で LVS（同梱の `ytr0_top.spice` がこのブロックの回路図側のネットリストです）
3. トップセル名の衝突。このブロックのセル名はすべて `ytr0_` で始まります（`ytr0_top` / `ytr0_inverter` / `ytr0_sram` / `art_ytr0`）。
   PCell 由来のセル（`fet_p` `fet_n` `cont_g` `cont_n` `cont_p`）は接頭辞なしです。
   ほかの設計と同名で中身が違う場合は、リネームして取り込んでください

## 同梱物

| ファイル | 中身 |
|---|---|
| `ytr0_top.gds` | レイアウト |
| `ytr0_top.spice` | LVS 用ネットリスト（回路図から生成） |
| `ytr0_top.sch` | 回路図（xschem） |
| `drc.lyrdb` | DRC の結果（違反 0 件） |
| `lvs.lvsdb` | LVS の結果（一致） |
| `MANIFEST.txt` | ファイルのハッシュと検証結果 |
| `PINLIST.md` | この文書 |

## 連絡先

ytr0（yu20@iamas.ac.jp）
